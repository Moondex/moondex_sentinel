# specify path to moondex.conf or leave blank
# default is the same as MoondexCore
#moondex_conf=/home/evan82/.moondexcore/moondex.conf

# valid options are mainnet, testnet (default=mainnet)
network=mainnet
#network=testnet

# database connection details
db_name=database/sentinel.db
db_driver=sqlite
